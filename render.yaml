services:
  - type: web
    name: minha-api
    env: node
    repo: https://github.com/seu-usuario/seu-repositorio
    branch: main
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production